/**
 * @deprecated This file will be removed in the next version
 * Use specific utility modules from './utils/' instead:
 * - ./utils/generic.js for cn(), type utilities
 * - ./utils/formatting.js for formatFileSize(), getFileExtension()
 * - ./utils/status.js for status configurations and helpers
 * - ./utils/dependencies.js for dependency checking
 */

// Re-export everything for backwards compatibility
export * from './utils/index.js';